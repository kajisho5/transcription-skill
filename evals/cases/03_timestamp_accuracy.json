{
  "id": "03_timestamp_accuracy",
  "input": "tests/fixtures/lecture_short.mp4",
  "request": {"word_timestamps": true},
  "reference": "lecture_short.mp4",
  "metric": "timestamps",
  "onset_tolerance": 1.5,
  "note": "Whisper attaches leading silence to the first segment, so onsets are checked with a 1.5 s tolerance; the second part's onset is derived from the first fixture's duration + the 1.0 s gap + its own 1.0 s lead."
}
